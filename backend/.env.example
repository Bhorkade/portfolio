# Server Port
PORT=5000

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# MongoDB Atlas Connection URI
# Example: mongodb+srv://<username>:<password>@cluster0.abcde.mongodb.net/portfolio?retryWrites=true&w=majority
MONGODB_URI=

# Email Notification Configuration (e.g. SendGrid, Mailgun, AWS SES, or standard SMTP)
EMAIL_API_KEY=
EMAIL_FROM=notifications@yourdomain.com
EMAIL_TO=bhorkadeshrikar154@gmail.com

# Optional SMTP Settings (defaults to SendGrid host if not specified)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
